services:
  - type: web
    name: engineering-loadboard
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: node server.js
    disk:
      name: loadboard-data
      mountPath: /app/data
      sizeGB: 1
